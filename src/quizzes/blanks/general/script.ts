`

# FULL NAME 
What is the capital of the Dominican Republic?
= @tx(Santo Domingo);

# 18..
When is the independence of the DR?
= dt(27/02/1844);

# +1
How many provinces does the DR have?
= nm(31);

# ??
The flag of the Dominican Republic is white, red and...
@cl(blue);

->

function getElementType(code: string): string {
  // parse char by char, we need to determine function,
  // content, and parse the content 
}

function pseudoParser(code: string) {
  
  const chunks = code.split(";");

  for (const chunk of chunks) {

    const label = ...
    const cleaned = ...
    
    const question, content = cleaned.split('='); 

    
    
  }
  
}


`

