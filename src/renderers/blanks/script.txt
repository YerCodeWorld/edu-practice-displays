import { parserContract } from "./parser";

import baseCSS from "./styles.css";
// not needed, declared to satisfy contract
const baseHTML = "";

interface BlanksData {

    dsl: string

}

function blanksRenderer({

    mount: HTMLElement,
    data: BlanksData

}): BlanksReturn {

    const ariaLabel = "...";
    const root = createSection("edu-blanks", ariaLabel);
    const p: HTMLParagraphElement;
    const container: HTMLDivElement;

    function init() {

        p = document.createElement("p");
        p.className = "edu-blanks-p";

        const pInnerHtml = parserContract.parseCode(data.dsl);
        p.innerHTML = pInnerHtml;

        container = document.createElement("div");
        container.className = "edu-blanks-container";
        container.appendChild(p);

        root.appendChild(container);
    }

    function finish() {}

    return {


        finish
    }

}

function blanksValidator() {}

export const BlanksContract = {

    name: "Fill Blanks",
    description: "",

    themes,
    version: 1.0,
    parserVersion: 1.0,

    category: "complete",
    tags: [],

    usage: [

    ],
    wrong: [

    ],

    exampleContent: [

    ],

    defaultOptions: {
        shuffle: true,
        allowRetry: false,
        ariaLabel: "Blanks Exercise"
    },

    implementation: {
        renderer: blanksRenderer,
        parser: parserContract.dslParser,
        validator: blanksValidator
    },

    html: baseHTML,
    css: baseCSS
}

